{
  "name": "projeto-inmetrics",
  "version": "1.0.0",
  "scripts": {
    "cypress:pre": "npx @badeball/cypress-cucumber-preprocessor generate-types",
    "test:ci": "npm run cypress:pre && cypress run --browser chrome --spec \"cypress/e2e/**/*.feature\""
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.1.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.5",
    "cypress": "^14.4.1",
    "esbuild": "^0.25.5"
  }
}
